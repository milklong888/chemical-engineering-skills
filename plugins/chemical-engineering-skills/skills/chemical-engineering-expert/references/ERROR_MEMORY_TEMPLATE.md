# Error Memory — <asset name>

Read verified entries before the asset's ordinary instructions. This memory has
procedural inspection priority but cannot override fresher project authority.

## <ID> — <short failure name>

- `scope`:
- `trigger`:
- `mistaken_behavior`:
- `invariant`:
- `detector`:
- `repair`:
- `evidence`:
- `authority`:
- `status`: pending | verified_by_user | verified | quarantined | superseded
- `priority`: P0 | P1 | P2
- `severity`: critical | high | medium | low
- `repeat_count`: 1
- `last_seen`: YYYY-MM-DD
- `supersedes`: none

