# 新知识待审入口

当前无新增候选。本文件是结构化治理入口，不是检索正文，也不把本次迁移或用户反馈提升为化工事实。

任务中保留本地审计；授权临时收件统一走中央expert的`references/EXPERIENCE_INBOX.md`，本入口不另设收件规则。用户明确发起整理且关闭当前任务版本后，才按中央正式晋升流程评估 `prompt_principle`（宏观原则、行事规则）或 `data_pattern`（可复验的数据规律）；没有新增可迁移价值时保持 `no_change`。

正式晋升候选必须记录来源及 SHA、原内容定位、适用范围、单位和前提、谱系与严格基线、反例、实测证据和待审状态。完整关闭证据、谱系门和候选质量门缺一不可；候选不等于晋升，不自动写 canonical 或默认检索。放宽案例及所有后代永不进入共享学习、成功案例或默认 RAG。重复次数只影响注意力，不增加技术事实权重。

新材料初始为 `candidate` 或 `quarantined`；不得把一份案例数值当通用规则，不得直接覆盖原节点、来源定位和正文。

- `default_retrieval_eligible`: false
- `learning_event`: false
- `candidates`: []

中央唯一责任为逻辑 owner `chemical-engineering-expert`；按 [安装无关治理路由](../../OWNER_ROUTES.md) 定位其真实进化/严格验收文件。先读本图谱既有错误入口，不另造规则 owner。

用户明确要求的外部资料增补可以在维护任务中登记X类来源候选，不必等待该资料维护任务结束。逐节点来源、许可和独立review必须覆盖全部变更；仅从任务经验提炼的两通道候选适用上述结束、谱系和质量门。未准入节点不进入默认检索向量，独立候选版本不等于发布或晋升。
