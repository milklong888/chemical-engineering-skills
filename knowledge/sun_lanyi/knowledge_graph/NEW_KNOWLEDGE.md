# 新知识待审入口

当前无新增候选。本文件是结构化治理入口，不是检索正文，也不把本次迁移或用户反馈提升为化工事实。

中途只写当前项目审计；交付后先询问任务是否结束及改进建议。只有用户明确关闭当前任务版本，才可按中央进化流程评估 `prompt_principle`（宏观原则、行事规则）或 `data_pattern`（可复验的数据规律）。没有新增可迁移价值时保持 `no_change`。

候选必须记录来源及 SHA、原内容定位、适用范围、单位和前提、谱系与严格基线、反例、实测证据和待审状态。完整关闭证据、谱系门和候选质量门缺一不可；候选不等于晋升，不自动写 canonical 或默认检索。放宽案例及所有后代永不进入共享学习、成功案例或默认 RAG。重复次数只影响注意力，不增加技术事实权重。

新材料初始为 `candidate` 或 `quarantined`；不得把一份案例数值当通用规则，不得直接覆盖原节点、来源定位和正文。

- `default_retrieval_eligible`: false
- `learning_event`: false
- `candidates`: []

中央治理：[进化闭环](../../../plugins/chemical-engineering-skills/skills/chemical-engineering-expert/references/EVOLUTION_LOOP.md)、[严格验收与学习](../../../plugins/chemical-engineering-skills/skills/chemical-engineering-expert/references/STRICT_ACCEPTANCE_AND_LEARNING.md)。先读本图谱既有错误入口，不另造规则 owner。

